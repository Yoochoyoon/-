## 개요

`01`~`08` 기획 문서를 바탕으로 실제 플레이 가능한 웹앱 MVP를 만들었다. 이 문서는 기획 문서에는 없는, 이번 개발 세션에서 정해지거나 알아둬야 할 것들만 정리한다(설치·실행법은 `README.md` 참고, 게임 규칙 자체는 `01`~`08` 참고).

## 프로젝트 위치가 바뀌었다

- 원래 기획 문서는 OneDrive(`C:\Users\cy060\OneDrive\집중표적게임`)에 있었지만, 코드(특히 `node_modules`)는 OneDrive 동기화 폴더와 궁합이 안 좋아서 **코드+문서+옵시디언 vault를 전부 `D:\집중표적게임`으로 통합 이동**했다. OneDrive 쪽 폴더는 이제 비어 있다.
- Claude Code를 다시 실행할 때는 **`D:\집중표적게임`에서 열어야** 경로가 맞는다. (이번 세션은 예전 OneDrive 경로를 기준 폴더로 잡은 채 시작됐지만, 파일 작업은 전부 절대경로로 `D:\집중표적게임`을 대상으로 했다.)

## 기술 스택 & 실제 구현 범위

- **스택**: Node.js + TypeScript + Express + Socket.IO 백엔드, 빌드 도구 없는 순수 HTML/CSS/JS 프론트(React 사용 안 함), Render.com 배포 예정.
- **상태 저장**: 메모리에만 저장 — DB 없음. 서버가 재시작되면 진행 중이던 모든 방이 초기화된다(MVP 한정 허용).
- **이번에 구현된 것**: 8명 고정 입장, 역할 자동배정, 닉네임 중복 방지, 1라운드 탐색 라운드(스파이 서로 정체 확인, 전투 없음), 2라운드부터 기본 공격 + 역할별 특수 능력(보스 긴급처형·경호원 육탄방어/충성심서약·스파이 교란작전·배신자 흑막의미소) 처리, 낮(결과공개 → 토론 → 투표), 동점 시 재투표, 승리조건 판정, 진행자의 타이머 강제진행·연장, 첫판(1~2라운드) 힌트 툴팁.
- **아직 구현 안 된 것** (기획 문서엔 있지만 코드엔 없음): 인원수 6~10명 가변, 스파이 "암둠의 공모"(밤중 실시간 합공 신호 UI), 배신자 "이중 스파이" 패시브(대응하는 "조사" 메커니즘 자체가 아직 없어 보류), 사망자 관전 UI, 재접속 자동 복귀.

## 코드 짜면서 새로 정한 것들 (기획 문서엔 없던 세부사항)

- **진행자(host)는 8명 역할에 포함되지 않는 별도 컨트롤러**로 구현했다. `06컴포넌트테크구현.md`에는 "진행자도 역할을 겸해 참가할 수 있다"고 돼 있지만, MVP에서는 진행자=관전+컨트롤 전용이고 겸직은 2차 작업으로 미뤘다.
- **TV 화면은 자동 연결이 아니라 방 코드를 직접 입력**해서 연결한다(추후 QR코드 등으로 개선 가능).
- **자기 자신은 밤 지목/낮 투표 대상에서 제외**된다(자기공격·자기투표 불가) — 문서에 명시되지 않아 상식적으로 정한 부분.
- **결과 공개(낮 데미지 공개) 페이즈는 타이머가 없다** — 진행자가 화면에서 "토론 시작하기" 버튼을 눌러야 다음(토론)으로 넘어간다.
- **방 코드는 헷갈리는 문자(0/O, 1/I)를 제외한 4자리**로 자동 생성된다.

## 능력별 세부 구현 결정 (04핵심메커니즘.md 기준으로 코드로 옮기며 확정한 것)

- **육탄 방어 절반 경감은 내림(floor)** 처리. 예: 데미지 3 → 절반 경감 시 1.
- **육탄 방어 쿨타임 "1라운드"** = 사용한 라운드(N)의 바로 다음 라운드(N+1)엔 사용 불가, N+2부터 재사용 가능.
- **교란 작전은 그 자리(같은 라운드)에서 대상의 밤 행동 전체를 무효화**한다 — 모든 행동이 동시 제출되므로, 서버가 방어/공격 단계보다 먼저 침묵 대상을 판정해 걸러내는 방식으로 구현.
- **흑막의 미소의 HP 회복은 "이번 라운드 밤 전투로 발생한 사망자 수 × 2"** 만 반영한다(낮 투표 사망은 별개 페이즈라 카운트하지 않음).
- **배신자 "이중 스파이" 패시브는 보류**: 원래 "조사 카드/능력을 받으면 50% 확률로 위장"이라는 규칙인데, 이 MVP엔 대응하는 "조사" 계열 능력 자체가 없어 트리거할 지점이 없다. 나중에 조사형 능력이 추가되면 같이 구현.
- **스파이 "암둠의 공모"(밤중 실시간 합공 신호)도 보류**: 정보 노출 UI라 별도 설계가 필요해 이번 스코프에서 제외.

## 검증한 것

- `resolveRound.ts`(밤 데미지 합산·능력 처리·낮 투표·승리조건 판정) 유닛 테스트 16개 통과.
- 타입체크(`npx tsc --noEmit`) 통과.
- `socket.io-client`로 8명+진행자를 실제 로컬 서버(`npm run dev`)에 접속시켜 방생성→입장→1라운드 정찰(스파이 상호 확인 확인)→2라운드 밤에 경호원 육탄방어(흡수)로 보스 피해를 대신 받는 것까지 소켓 통신으로 end-to-end 검증(스크립트는 검증 후 삭제, 커밋 안 됨).
- 브라우저 여러 탭으로 8명 풀 플레이(HP 버그 수정 검증 때 진행)는 특수 능력 추가 "이전" 버전 기준이었음 — 특수 능력이 들어간 지금 버전은 아직 브라우저로 직접 눈으로 확인 안 했다.

## 다음에 할 일

1. Render.com 배포 (`README.md`의 배포 순서 참고) — 사용자가 직접 진행 가능할 때로 보류 중
2. 남은 2차 기능: 인원수 6~10명 가변, 재접속 자동 복귀, 사망자 관전 UI
